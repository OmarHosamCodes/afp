import '../../../library.dart';

class AffichePage extends StatelessWidget {
  const AffichePage({
    super.key,
    required this.width,
    required this.height,
  });
  final double height;
  final double width;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
