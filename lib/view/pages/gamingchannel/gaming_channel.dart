import '../../../library.dart';

class GamingChannelPage extends StatelessWidget {
  const GamingChannelPage({
    super.key,
    required this.width,
    required this.height,
  });
  final double height;
  final double width;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
