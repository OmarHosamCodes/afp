import '../../../library.dart';

class DarkSidePage extends StatelessWidget {
  const DarkSidePage({
    super.key,
    required this.width,
    required this.height,
  });
  final double height;
  final double width;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
